# AxBot REST Book & Docs Guidelines

- **Documentation Framework**: Code for docs targets VuePress v1.x configurations.
- **Markdown Style**: Keep lines clean, use clear headings, and always syntax highlight codeblocks.
- **Docker**: For infrastructure edits, note the use of `docker-compose.yaml` and `Dockerfile` based on `autoxing/axbot_rest_book`.
